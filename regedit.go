package main

import (
	"net/http"
)

func IndexHandler(response http.ResponseWriter, r *http.Request) {
	//s, err := ui.Regedit()
	//if err != nil {
	//	response.Write([]byte(err.Error()))
	//	return
	//}
	//response.Write([]byte(s))
}
